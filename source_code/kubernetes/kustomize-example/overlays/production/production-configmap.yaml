apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  environment: "production"
  log_level: "warn"
  database_host: "postgres-prod-cluster"
  database_port: "5432"
  cache_enabled: "true"
  max_connections: "500"
  ssl_enabled: "true"
  backup_enabled: "true"

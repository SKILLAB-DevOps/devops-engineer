[project]
name = "python_microservice"
version = "0.1.0"
description = ""
authors = [
    { name="Your Name", email="your.email@example.com" }
]
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "psycopg2-binary"
]

[project.optional-dependencies]
dev = [
    "pylint",
    "pytest",
    "mypy",
    "black",
    "coverage"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"